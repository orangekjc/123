{
  "version": 2,
  "buildCommand": "nest build",
  "builds": [
    {
      "src": "src/app.ts",
      "use": "@vercel/node"
    }
  ],
  "outputDirectory": "/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }]
}
